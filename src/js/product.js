import data from "data.js"

const products={
    render:()=>{
        const {products}=data;
        return `<ul class="products">
        ${products.map((product)=>`
        <li>
        <div>
        )}
    }
}
